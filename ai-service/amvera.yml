meta:
  environment: python
  toolchain:
    name: pip
    version: 3.12.4
build:
  requirementsPath: requirements.txt
run:
  command: uvicorn main:app --reload --port 5001
  persistenceMount: /data
  containerPort: 5001
